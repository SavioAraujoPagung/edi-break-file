version: "3.7"

services:
  bd:
    image: postgres:9.4
    restart: always
    tty: true
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTEGRES_PASSWORD=root
      - POSTGRES_DB=break_file_db_dev
    ports:
      - 5413:5432